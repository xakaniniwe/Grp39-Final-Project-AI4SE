services:
  - type: web
    name: SakhaSonke
    env: python
    plan: free
    region: oregon
    branch: main
    runtime: python
    pythonVersion: 3.10.13
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeploy: true